[build]
  command = "npm run build"

[build.environment]
  DISABLE_NETLIFY_TOOLBAR = "true"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
